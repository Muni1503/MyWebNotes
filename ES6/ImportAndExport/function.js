//!Here we are going to write a function and export it

let exp=(name,age)=> `Hello ${name} ,your age is ${age}`

//?export default exp;
//Unexpected token 'export' instead of export we use

module.exports={exp}